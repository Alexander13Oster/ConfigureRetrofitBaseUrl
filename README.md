# Different ways for changing the retrofit base url

Change the base url by:
- Flavor constant with configuration by build config and by flavor folder structure
- Manifest metadata; useful for SDK's
- Runtime interceptor